/**
 * Asset imports barrel file.
 * Import and re-export images, SVGs, and other static assets here.
 *
 * Example:
 * export { default as logoSvg } from './logo.svg'
 * export { default as heroImage } from './hero.webp'
 */
