import React from 'react';

/**
 * CustomCursor component
 * Note: Native system cursor is used for optimal performance and responsiveness across devices.
 */
export const CustomCursor: React.FC = () => {
  return null;
};

export default CustomCursor;
